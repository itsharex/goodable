# 飞书应用配置
APP_ID=你的App ID
APP_SECRET=你的App Secret
UserAccessToken=

# 应用配置
DEBUG=True

# 重要提示：
# 1. 重定向 URL 配置：
#    应用会自动识别实际运行端口，无需手动设置 PORT 变量
#    在飞书平台配置时，建议添加多个端口的回调 URL：
#    - http://localhost:5000/api/config/callback（本地开发）
#    - http://localhost:3100/api/config/callback（Goodable 平台）
#    - http://localhost:3101/api/config/callback（Goodable 平台）
#    - http://localhost:3102/api/config/callback（Goodable 平台）
#
# 2. UserAccessToken 会在授权成功后自动保存到此文件
